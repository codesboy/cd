就是说格式是
$arr = array(
    'uid'     => 1,
    'name' => '111',
)
$arr = array(
    'data1' = array(
        'uid'     =>１，
　　'name' => '111'
    ),
    'data2' => array(
        'uid'     => 2,
        'name' => '1111',
    )
)
二维关联数组
以上2种数据格式

$validate = new Validate($rule, $msg);
foreach($data as $value){
    $result   = $validate->check($value);
    if(!$result){
        return $validate->getError()
    }
}

导入积分数据
INSERT INTO client_credit_users (id,name,age,sex,telephone,pid,create_time,update_time) SELECT uid,memname,age,sex,memtel,parent_id,addtime,addtime FROM cdbc_member_extend

INSERT INTO client_credit_consumption (uid,disease_id,account_payable,used_credit,real_pay,get_credit,comment,pay_time,create_time,update_time) SELECT mem_name_id,lastill,account_payable,used_points,real_pay,get_points,notes,NULL,addtime,addtime FROM cdbc_member

年龄的计算

一个用户属于一个用户组(就是角色)
一个用户组包含多个用户
不同的用户所显示的菜单功能不一样
控制用户的权限就是设置用户组的权限
我这样理解的

还有一种是用户 角色 部门 这种应该更复杂